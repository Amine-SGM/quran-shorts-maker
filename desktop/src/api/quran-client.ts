// Stub API clients
export async function getChapters(language: string): Promise<any[]> {
  return [];
}

export async function getVersesByChapter(chapter: number, language: string, range: string): Promise<any[]> {
  return [];
}

export async function getReciters(language: string): Promise<any[]> {
  return [];
}
